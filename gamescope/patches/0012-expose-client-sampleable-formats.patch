diff --git a/src/ClientFormatPolicy.h b/src/ClientFormatPolicy.h
new file mode 100644
index 0000000..914b00d
--- /dev/null
+++ b/src/ClientFormatPolicy.h
@@ -0,0 +1,19 @@
+#pragma once
+
+namespace gamescope
+{
+    // Client buffers only need to be sampleable by the Vulkan compositor.
+    // Backends may impose a stricter modifier set for direct scanout. Keep
+    // that historical intersection unless a platform explicitly opts in.
+    constexpr bool ShouldAdvertiseSampleableClientModifier(
+        bool bAllowDeferredBackend,
+        bool bExposeClientSampleableFormats,
+        bool bBackendUsesModifiers,
+        bool bBackendSupportsModifier )
+    {
+        return bAllowDeferredBackend ||
+            bExposeClientSampleableFormats ||
+            !bBackendUsesModifiers ||
+            bBackendSupportsModifier;
+    }
+}
diff --git a/src/main.cpp b/src/main.cpp
index fe2f419..133b876 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -53,6 +53,7 @@ const char *g_pOriginalDisplay = nullptr;
 const char *g_pOriginalWaylandDisplay = nullptr;
 
 bool g_bAllowDeferredBackend = false;
+bool g_bExposeClientSampleableFormats = false;
 
 int g_nCursorScaleHeight = -1;
 
@@ -157,6 +158,7 @@ const struct option *gamescope_options = (struct option[]){
 	{ "mura-map", required_argument, nullptr, 0 },
 
 	{ "allow-deferred-backend", no_argument, nullptr, 0 },
+	{ "expose-client-sampleable-formats", no_argument, nullptr, 0 },
 	{ "keep-alive", no_argument, nullptr, 0 },
 
 	{} // keep last
@@ -276,6 +278,8 @@ const char usage[] =
 	"\n"
 	"Platform options:\n"
 	"  --allow-deferred-backend       Allows initting the backend in a deferred way, if it doesn't work immediately. (Note: This has some very minor correctness compromises that you should consider wrt. your platform with modifiers, etc).\n"
+	"  --expose-client-sampleable-formats\n"
+	"                                 Advertise Vulkan-sampleable client DMA-BUF formats even when the output backend cannot scan them out directly.\n"
 	"  --keep-alive                   Keep Gamescope alive even when the primary process has died.\n"
 	"\n"
 	"Keyboard shortcuts:\n"
@@ -832,6 +836,8 @@ int main(int argc, char **argv)
 					g_bLaunchMangoapp = true;
 				} else if (strcmp(opt_name, "allow-deferred-backend") == 0) {
 					g_bAllowDeferredBackend = true;
+				} else if (strcmp(opt_name, "expose-client-sampleable-formats") == 0) {
+					g_bExposeClientSampleableFormats = true;
 				} else if (strcmp(opt_name, "keep-alive") == 0) {
 					cv_shutdown_on_primary_child_death = false;
 				} else if (strcmp(opt_name, "virtual-connector-strategy") == 0) {
diff --git a/src/rendervulkan.cpp b/src/rendervulkan.cpp
index 0be72f6..8049efa 100644
--- a/src/rendervulkan.cpp
+++ b/src/rendervulkan.cpp
@@ -33,6 +33,7 @@
 #include "wlr_end.hpp"
 
 #include "rendervulkan.hpp"
+#include "ClientFormatPolicy.h"
 #include "main.hpp"
 #include "steamcompmgr.hpp"
 #include "log.hpp"
@@ -57,6 +58,7 @@
 
 extern bool g_bWasPartialComposite;
 extern bool g_bAllowDeferredBackend;
+extern bool g_bExposeClientSampleableFormats;
 
 static constexpr mat3x4 g_rgb2yuv_srgb_to_bt601_limited = {{
   { 0.257f, 0.504f, 0.098f, 0.0625f },
@@ -2876,11 +2878,16 @@ bool vulkan_init_format(VkFormat format, uint32_t drmFormat)
 				continue;
 			}
 
-			// The deferred backend exposes all sample-able formats as supported modifiers.
-			if ( !g_bAllowDeferredBackend )
+			const bool bBackendUsesModifiers = GetBackend()->UsesModifiers();
+			const bool bBackendSupportsModifier = !bBackendUsesModifiers ||
+				gamescope::Algorithm::Contains( GetBackend()->GetSupportedModifiers( drmFormat ), modifier );
+			if ( !gamescope::ShouldAdvertiseSampleableClientModifier(
+				g_bAllowDeferredBackend,
+				g_bExposeClientSampleableFormats,
+				bBackendUsesModifiers,
+				bBackendSupportsModifier ) )
 			{
-				if ( GetBackend()->UsesModifiers() && !gamescope::Algorithm::Contains( GetBackend()->GetSupportedModifiers( drmFormat ), modifier ) )
-					continue;
+				continue;
 			}
 
 			wlr_drm_format_set_add( &sampledDRMFormats, drmFormat, modifier );
@@ -2892,8 +2899,17 @@ bool vulkan_init_format(VkFormat format, uint32_t drmFormat)
 	}
 	else
 	{
-		if ( GetBackend()->UsesModifiers() && !GetBackend()->SupportsInvalidModifier( drmFormat ) )
+		const bool bBackendUsesModifiers = GetBackend()->UsesModifiers();
+		const bool bBackendSupportsInvalidModifier = !bBackendUsesModifiers ||
+			GetBackend()->SupportsInvalidModifier( drmFormat );
+		if ( !gamescope::ShouldAdvertiseSampleableClientModifier(
+			g_bAllowDeferredBackend,
+			g_bExposeClientSampleableFormats,
+			bBackendUsesModifiers,
+			bBackendSupportsInvalidModifier ) )
+		{
 			return false;
+		}
 
 		wlr_drm_format_set_add( &sampledDRMFormats, drmFormat, DRM_FORMAT_MOD_INVALID );
 		return false;
@@ -2902,6 +2918,11 @@ bool vulkan_init_format(VkFormat format, uint32_t drmFormat)
 
 bool vulkan_init_formats()
 {
+	if ( g_bExposeClientSampleableFormats )
+	{
+		vk_log.infof( "exposing Vulkan-sampleable client DMA-BUF formats without the output scanout intersection" );
+	}
+
 	for ( size_t i = 0; s_DRMVKFormatTable[i].DRMFormat != DRM_FORMAT_INVALID; i++ )
 	{
 		if (s_DRMVKFormatTable[i].internal)
diff --git a/tests/meson.build b/tests/meson.build
index 181cf0f..187db54 100644
--- a/tests/meson.build
+++ b/tests/meson.build
@@ -9,7 +9,7 @@ unittest_src += srcdir / 'edid.cpp'
 
 test_convar = executable(
 	'test_convar',
-	unittest_src + ['test_convar.cpp', 'test_display_lookup.cpp', 'test_drm_composition.cpp', 'test_edid.cpp'],
+	unittest_src + ['test_client_format_policy.cpp', 'test_convar.cpp', 'test_display_lookup.cpp', 'test_drm_composition.cpp', 'test_edid.cpp'],
 	include_directories: [srcdir, sol2_include],
 	dependencies: [catch2_dep, luajit_dep, cap_dep, displayinfo_dep, drm_dep, glm_dep, wlroots_dep],
 	cpp_args: gamescope_cpp_args,
diff --git a/tests/test_client_format_policy.cpp b/tests/test_client_format_policy.cpp
new file mode 100644
index 0000000..bf1557f
--- /dev/null
+++ b/tests/test_client_format_policy.cpp
@@ -0,0 +1,18 @@
+#include <catch2/catch_test_macros.hpp>
+
+#include "ClientFormatPolicy.h"
+
+using namespace gamescope;
+
+TEST_CASE("client modifier advertisement keeps the backend intersection by default", "[vulkan][dmabuf]")
+{
+    REQUIRE_FALSE( ShouldAdvertiseSampleableClientModifier( false, false, true, false ) );
+    REQUIRE( ShouldAdvertiseSampleableClientModifier( false, false, true, true ) );
+    REQUIRE( ShouldAdvertiseSampleableClientModifier( false, false, false, false ) );
+}
+
+TEST_CASE("sampleable-client opt-in bypasses only the backend advertisement filter", "[vulkan][dmabuf]")
+{
+    REQUIRE( ShouldAdvertiseSampleableClientModifier( false, true, true, false ) );
+    REQUIRE( ShouldAdvertiseSampleableClientModifier( true, false, true, false ) );
+}
