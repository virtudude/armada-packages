// stb_truetype implementation in its own translation unit, so its warnings stay
// out of armada-splash.c. Built with -w. See stb_truetype.h (public domain).
#define STB_TRUETYPE_IMPLEMENTATION
#include "stb_truetype.h"
